intents:
  - order
  - orderFood

actions:
  - respond_orderFood
  

responses:
  utter_order:
  - text: "你好，請問要點些什麼呢？"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
